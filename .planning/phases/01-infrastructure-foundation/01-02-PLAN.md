---
phase: 01-infrastructure-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/vite.config.ts
  - frontend/tailwind.config.ts
  - frontend/postcss.config.js
  - frontend/components.json
  - frontend/index.html
  - frontend/src/index.css
  - frontend/src/main.tsx
  - frontend/src/vite-env.d.ts
  - frontend/src/lib/utils.ts
  - frontend/src/routes/__root.tsx
  - frontend/src/routes/index.tsx
  - frontend/src/routes/login.tsx
  - frontend/src/routes/_auth.tsx
  - frontend/src/routes/_auth.dashboard.tsx
  - frontend/src/routes/_auth.demands.tsx
  - frontend/src/routes/_auth.candidates.tsx
  - frontend/src/routes/_auth.contracts.tsx
  - frontend/src/routes/_auth.profiles.tsx
  - frontend/src/routes/_auth.approvals.tsx
  - frontend/src/routes/_auth.sla.tsx
  - frontend/src/routes/_auth.admin.tsx
autonomous: true
requirements: [INFRA-01]
has_tests: false
reason_no_tests: "Frontend scaffold with placeholder routes and copied design system files — no business logic to test. Future phases add React Testing Library when components have behavior."
locked_decisions:
  - "React 19 + Vite + TypeScript, not React 18 or CRA or Next.js"
  - "TanStack Router with file-based routing via @tanstack/router-vite-plugin, not React Router DOM"
  - "TanStack Router Vite plugin MUST be listed BEFORE react() plugin in vite.config.ts"
  - "EUROCONTROL design system files copied verbatim from /home/przem/l1-service-desk-automation/frontend/"
  - "Shadcn/UI with Tailwind CSS v3, not Tailwind v4"
  - "Exo font from Google Fonts as default font-sans"
  - "Desktop-only — min 1280px viewport, no mobile breakpoints"
verification_must_check:
  - "TanStackRouterVite plugin declared BEFORE react() in vite.config.ts plugins array"
  - "routeTree.gen.ts is auto-generated (not manually created)"
  - "index.css contains --ec-primary, --ec-secondary, --ec-accent HSL variables"
  - "tailwind.config.ts contains ec.primary, ec.secondary, ec.accent color tokens"
  - "Exo font imported in index.css"
  - "No react-router-dom dependency in package.json"
  - "No Tailwind v4 — must be tailwindcss ^3.4"
  - "cn() utility uses clsx + tailwind-merge"

must_haves:
  truths:
    - "npm run dev starts Vite dev server without errors"
    - "npm run build produces dist/ without errors"
    - "Browser at localhost:3000 shows the root route with EUROCONTROL styling (Exo font, EC colors)"
    - "Navigating to /dashboard shows a placeholder page without 404"
    - "Navigating to /demands, /candidates, /contracts, /profiles, /approvals, /sla, /admin all render placeholder content without 404"
    - "Shadcn/UI Button component renders with EUROCONTROL color tokens"
    - "routeTree.gen.ts is auto-generated when dev server starts"
  artifacts:
    - path: "frontend/vite.config.ts"
      provides: "Vite config with TanStack Router plugin + path aliases"
      contains: "TanStackRouterVite"
    - path: "frontend/src/index.css"
      provides: "EUROCONTROL CSS variables + Exo font + utility classes"
      contains: "--ec-primary"
    - path: "frontend/tailwind.config.ts"
      provides: "Tailwind config with EC color tokens"
      contains: "ec-primary"
    - path: "frontend/components.json"
      provides: "Shadcn/UI configuration"
      contains: "shadcn"
    - path: "frontend/src/main.tsx"
      provides: "React 19 entry point with TanStack RouterProvider"
      contains: "RouterProvider"
    - path: "frontend/src/routes/__root.tsx"
      provides: "Root layout with Outlet"
      contains: "createRootRoute"
    - path: "frontend/src/lib/utils.ts"
      provides: "cn() utility function"
      contains: "twMerge"
    - path: "frontend/src/routes/_auth.dashboard.tsx"
      provides: "Dashboard placeholder route"
      contains: "createFileRoute"
  key_links:
    - from: "frontend/src/main.tsx"
      to: "frontend/src/routeTree.gen.ts"
      via: "import for router creation"
      pattern: "routeTree\\.gen"
    - from: "frontend/vite.config.ts"
      to: "frontend/src/routes/"
      via: "TanStack Router Vite plugin watches routes directory"
      pattern: "TanStackRouterVite"
    - from: "frontend/tailwind.config.ts"
      to: "frontend/src/index.css"
      via: "CSS variable references in color tokens"
      pattern: "hsl\\(var\\(--ec-"
---

<objective>
Create the complete React 19 frontend scaffold with TanStack Router file-based routing, the EUROCONTROL design system (copied verbatim from the reference project), Shadcn/UI components, and placeholder routes for every top-level navigation destination.

Purpose: Establishes the frontend foundation with the exact EUROCONTROL visual identity. Every subsequent phase adds pages and components on top of this scaffold. The design system must be pixel-identical to the reference project.

Output: A working Vite dev server serving a React 19 SPA with TanStack Router, EUROCONTROL branding (Exo font, EC color palette), Shadcn/UI base components, and placeholder routes for all top-level sections.
</objective>

<execution_context>
@/home/przem/.claude/forge-cli/workflows/execute-plan.md
@/home/przem/.claude/forge-cli/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md

Design system source files (copy verbatim):
@/home/przem/l1-service-desk-automation/frontend/src/index.css
@/home/przem/l1-service-desk-automation/frontend/tailwind.config.ts
@/home/przem/l1-service-desk-automation/frontend/components.json
@/home/przem/l1-service-desk-automation/frontend/postcss.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React 19 + TypeScript project with TanStack Router</name>
  <files>
    frontend/package.json
    frontend/tsconfig.json
    frontend/tsconfig.app.json
    frontend/tsconfig.node.json
    frontend/vite.config.ts
    frontend/index.html
    frontend/src/main.tsx
    frontend/src/vite-env.d.ts
  </files>
  <action>
    Initialize the frontend project from scratch (do NOT use `npm create vite` — manually create all files for full control).

    **package.json** — Create with name "bryton-ai-cv-app", scripts (dev, build, preview, lint), and these dependencies:
    - react: "^19.0.0", react-dom: "^19.0.0"
    - @tanstack/react-router: "^1.0.0", @tanstack/router-vite-plugin: "^1.0.0"
    - class-variance-authority: "^0.7.0", clsx: "^2.1.0", tailwind-merge: "^2.6.0"
    - lucide-react: "^0.469.0", @radix-ui/react-slot: "^1.1.0"
    - @tanstack/react-query: "^5.0.0" (for future phases)
    - zustand: "^5.0.0" (for future phases)
    - react-hook-form: "^7.0.0", zod: "^3.0.0" (for future phases)
    DevDependencies:
    - @vitejs/plugin-react: "^4.0.0", vite: "^6.0.0", typescript: "^5.7.0"
    - tailwindcss: "^3.4.0", autoprefixer: "^10.4.0", postcss: "^8.4.0", tailwindcss-animate: "^1.0.7"
    - @types/react: "^19.0.0", @types/react-dom: "^19.0.0"
    - eslint, @eslint/js, typescript-eslint, eslint-plugin-react-hooks

    Run `cd frontend && npm install` after creating package.json. Use `--legacy-peer-deps` if peer dep warnings appear for React 19.

    **vite.config.ts** — CRITICAL: TanStackRouterVite MUST be FIRST in plugins array, BEFORE react(). Include:
    - TanStackRouterVite({ target: 'react', autoCodeSplitting: true })
    - react()
    - resolve.alias: '@' -> path.resolve(__dirname, './src')
    - server.host: '0.0.0.0', server.port: 3000
    - server.proxy: '/api' -> 'http://backend:8000' (for Docker Compose)

    **tsconfig.json** — References to tsconfig.app.json and tsconfig.node.json.

    **tsconfig.app.json** — target ES2020, lib ["ES2020", "DOM", "DOM.Iterable"], module ESNext, moduleResolution bundler, jsx react-jsx, strict true, paths: {"@/*": ["./src/*"]}.

    **tsconfig.node.json** — For Vite config files.

    **index.html** — Standard Vite HTML with `<div id="root"></div>`, title "Bryton AI CV App".

    **src/main.tsx** — Following RESEARCH.md Pattern 1 exactly:
    - Import RouterProvider, createRouter from @tanstack/react-router
    - Import routeTree from ./routeTree.gen
    - Create router with createRouter({ routeTree })
    - declare module '@tanstack/react-router' for type registration
    - Render in StrictMode with RouterProvider

    **src/vite-env.d.ts** — Vite client types reference.
  </action>
  <verify>
    cd frontend && npm run build 2>&1 | head -20
    Verify build succeeds (routeTree.gen.ts will be auto-generated during build).
  </verify>
  <done>
    Vite + React 19 + TypeScript + TanStack Router project scaffolded with all config files. npm install completed. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: EUROCONTROL design system, Shadcn/UI, placeholder routes</name>
  <files>
    frontend/src/index.css
    frontend/tailwind.config.ts
    frontend/postcss.config.js
    frontend/components.json
    frontend/src/lib/utils.ts
    frontend/src/routes/__root.tsx
    frontend/src/routes/index.tsx
    frontend/src/routes/login.tsx
    frontend/src/routes/_auth.tsx
    frontend/src/routes/_auth.dashboard.tsx
    frontend/src/routes/_auth.demands.tsx
    frontend/src/routes/_auth.candidates.tsx
    frontend/src/routes/_auth.contracts.tsx
    frontend/src/routes/_auth.profiles.tsx
    frontend/src/routes/_auth.approvals.tsx
    frontend/src/routes/_auth.sla.tsx
    frontend/src/routes/_auth.admin.tsx
  </files>
  <action>
    **Step 1 — Copy EUROCONTROL design system files verbatim:**

    Copy these 4 files EXACTLY from the reference project (do not modify):
    - /home/przem/l1-service-desk-automation/frontend/src/index.css -> frontend/src/index.css
    - /home/przem/l1-service-desk-automation/frontend/tailwind.config.ts -> frontend/tailwind.config.ts
    - /home/przem/l1-service-desk-automation/frontend/components.json -> frontend/components.json
    - /home/przem/l1-service-desk-automation/frontend/postcss.config.js -> frontend/postcss.config.js

    After copying, verify these exist in index.css:
    - `@import url('https://fonts.googleapis.com/css2?family=Exo:wght@300;400;500;600;700&display=swap')`
    - `--ec-primary: 210 100% 20%`
    - `--ec-secondary: 211 82% 54%`
    - `--ec-accent: 195 100% 37%`

    After copying, verify tailwind.config.ts contains `ec` color tokens using `hsl(var(--ec-*))` pattern and Exo in fontFamily.sans.

    **Step 2 — Install Shadcn/UI base components:**

    Run: `cd frontend && npx shadcn@latest add button card badge input label separator --yes`

    This creates src/components/ui/ with the Shadcn components configured per components.json.

    **Step 3 — Create cn() utility:**

    frontend/src/lib/utils.ts:
    ```
    import { type ClassValue, clsx } from "clsx"
    import { twMerge } from "tailwind-merge"
    export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)) }
    ```

    **Step 4 — Create routes:**

    frontend/src/routes/__root.tsx:
    - createRootRoute with component rendering `<Outlet />`
    - Import and render a minimal layout wrapper: a div with className "min-h-screen bg-background font-sans antialiased" wrapping the Outlet
    - Import index.css in __root.tsx (or ensure it's imported in main.tsx)

    frontend/src/routes/index.tsx:
    - createFileRoute('/') that redirects to /dashboard using `beforeLoad: () => { throw redirect({ to: '/dashboard' }) }`
    - Import redirect from @tanstack/react-router

    frontend/src/routes/login.tsx:
    - createFileRoute('/login') with a placeholder login page
    - Show "Bryton AI CV App" heading with Exo font, centered, with ec-primary color
    - Include a Shadcn Button component to verify it renders with EUROCONTROL styling

    frontend/src/routes/_auth.tsx:
    - Pathless layout route using createFileRoute('/_auth')
    - Component renders a simple layout shell: left sidebar placeholder (dark navy background with ec-header-gradient class) and main content area with Outlet
    - Sidebar should show "Bryton AI" branding and placeholder nav links to all child routes
    - This layout will be expanded in Phase 2 with real auth guards

    frontend/src/routes/_auth.dashboard.tsx through _auth.admin.tsx:
    - Each file creates a route like createFileRoute('/_auth/dashboard')
    - Each renders a placeholder page with:
      - h1 with the section name (e.g., "Dashboard", "Demands", "Candidates") using text-2xl font-bold text-foreground
      - p with "Coming in Phase N." using text-sm text-muted-foreground
    - Routes to create:
      - _auth.dashboard.tsx → "Dashboard" (Phase 2)
      - _auth.demands.tsx → "Demands" (Phase 5)
      - _auth.candidates.tsx → "Candidates" (Phase 4)
      - _auth.contracts.tsx → "Contracts" (Phase 3)
      - _auth.profiles.tsx → "Profile Catalogue" (Phase 3)
      - _auth.approvals.tsx → "Approvals" (Phase 2)
      - _auth.sla.tsx → "SLA Dashboard" (Phase 9)
      - _auth.admin.tsx → "Admin" (Phase 2)

    **Design for the _auth layout sidebar:**
    - Width: w-64
    - Background: ec-header-gradient class (dark blue gradient)
    - Logo area: "Bryton AI" in text-xl font-bold text-white with an ec-accent underline
    - Nav links: text-blue-200 hover:text-white with ec-nav-active class on current route
    - Use Link from @tanstack/react-router for navigation
    - Each link shows the section name and uses the correct `to` prop
  </action>
  <verify>
    - `cd frontend && npm run build` succeeds
    - `grep "ec-primary" frontend/src/index.css` finds the CSS variable
    - `grep "ec-primary" frontend/tailwind.config.ts` finds the Tailwind token
    - `grep "Exo" frontend/src/index.css` finds the font import
    - `ls frontend/src/components/ui/button.tsx` confirms Shadcn Button exists
    - `ls frontend/src/routes/_auth.dashboard.tsx` confirms route file exists
    - `ls frontend/src/routeTree.gen.ts` confirms auto-generation worked
  </verify>
  <done>
    EUROCONTROL design system copied verbatim (index.css, tailwind.config.ts, components.json, postcss.config.js). Shadcn/UI base components installed (button, card, badge, input, label, separator). All top-level routes created as placeholders with TanStack Router file-based routing. Auth layout shell with EUROCONTROL-branded sidebar renders correctly. Build succeeds. routeTree.gen.ts auto-generated.
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npm run dev` starts without errors
- `cd frontend && npm run build` produces dist/ without errors
- Browser at localhost:3000 shows EUROCONTROL-styled page with Exo font
- All routes render placeholder content (no 404s)
- Shadcn/UI Button renders with correct styling
- CSS variables (--ec-primary, --ec-secondary, --ec-accent) present in index.css
- routeTree.gen.ts exists and contains all route definitions
- No react-router-dom in node_modules
</verification>

<success_criteria>
- Frontend dev server starts and serves all routes without errors
- Production build succeeds
- EUROCONTROL design system visually applied (Exo font, EC colors)
- Every top-level route renders a placeholder without 404
- Shadcn/UI components render correctly with HSL custom properties
- TanStack Router type-safe navigation works between routes
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-02-SUMMARY.md`
</output>
