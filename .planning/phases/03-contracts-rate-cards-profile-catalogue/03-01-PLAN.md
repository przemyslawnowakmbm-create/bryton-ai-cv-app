---
phase: 03-contracts-rate-cards-profile-catalogue
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/contract.py
  - backend/app/models/rate_card.py
  - backend/app/models/profile_catalogue.py
  - backend/app/models/profile_requirement.py
  - backend/app/models/demand.py
  - backend/app/models/__init__.py
  - backend/app/schemas/contract.py
  - backend/app/schemas/profile.py
  - backend/alembic/versions/005_contracts_profiles.py
autonomous: true
requirements: [CONTRACT-01, CONTRACT-02, CONTRACT-04, PROFILE-01]
has_tests: false
reason_no_tests: "Data model + migration plan — no business logic to test; models are exercised by Plan 02 and 03 API tests"

must_haves:
  truths:
    - "All 4 new tables (contracts, rate_cards, profile_catalogue, profile_requirements) exist in the database after migration"
    - "Demand stub table exists with profile_id FK and profile_snapshot JSONB column"
    - "Pydantic schemas validate contract and rate card create/update/response payloads"
    - "Pydantic schemas validate profile catalogue and requirement create/update/response payloads"
    - "All new models are importable from app.models"
  artifacts:
    - path: "backend/app/models/contract.py"
      provides: "Contract ORM model"
      contains: "class Contract"
    - path: "backend/app/models/rate_card.py"
      provides: "RateCardEntry ORM model with UNIQUE constraint"
      contains: "class RateCardEntry"
    - path: "backend/app/models/profile_catalogue.py"
      provides: "ProfileCatalogue ORM model"
      contains: "class ProfileCatalogue"
    - path: "backend/app/models/profile_requirement.py"
      provides: "ProfileRequirement ORM model"
      contains: "class ProfileRequirement"
    - path: "backend/app/models/demand.py"
      provides: "Demand stub ORM model with profile_snapshot JSONB"
      contains: "class Demand"
    - path: "backend/app/schemas/contract.py"
      provides: "Contract and rate card Pydantic schemas"
      exports: ["ContractCreate", "ContractUpdate", "ContractResponse", "RateCardEntryCreate", "RateCardEntryUpdate", "RateCardEntryResponse"]
    - path: "backend/app/schemas/profile.py"
      provides: "Profile and requirement Pydantic schemas"
      exports: ["ProfileCreate", "ProfileUpdate", "ProfileResponse", "ProfileRequirementCreate", "ProfileRequirementResponse", "DemandDefaultsResponse"]
    - path: "backend/alembic/versions/005_contracts_profiles.py"
      provides: "Migration creating all Phase 3 tables with RLS"
      contains: "def upgrade"
  key_links:
    - from: "backend/app/models/rate_card.py"
      to: "backend/app/models/contract.py"
      via: "ForeignKey('contracts.id')"
      pattern: "ForeignKey.*contracts\\.id"
    - from: "backend/app/models/rate_card.py"
      to: "backend/app/models/profile_catalogue.py"
      via: "ForeignKey('profile_catalogue.id')"
      pattern: "ForeignKey.*profile_catalogue\\.id"
    - from: "backend/app/models/profile_requirement.py"
      to: "backend/app/models/profile_catalogue.py"
      via: "ForeignKey('profile_catalogue.id')"
      pattern: "ForeignKey.*profile_catalogue\\.id"
    - from: "backend/app/models/demand.py"
      to: "backend/app/models/profile_catalogue.py"
      via: "ForeignKey('profile_catalogue.id')"
      pattern: "ForeignKey.*profile_catalogue\\.id"

locked_decisions:
  - "Use sfia_level INTEGER with CHECK (1-7) on rate_cards — NOT a UUID FK to sfia_levels table"
  - "Use Numeric(10,2) for max_daily_rate — never float"
  - "Demands table is a STUB with only Phase 3 fields (id, tenant_id, profile_id, profile_snapshot, created_at, updated_at) — Phase 5 adds remaining columns"
  - "profile_requirements gets a denormalised tenant_id column for RLS — not relying on JOIN-based policies"
  - "Table name is rate_cards (not rate_card_entries) matching ARCHITECTURE.md"
  - "UNIQUE(contract_id, profile_id, sfia_level, effective_from) on rate_cards"
  - "Margin is NEVER stored — computed at read time from ceiling minus cost_rate"

verification_must_check:
  - "rate_cards.sfia_level is INTEGER with CHECK constraint, not UUID FK"
  - "rate_cards.max_daily_rate uses Numeric(10,2)"
  - "demands table has profile_snapshot JSONB column"
  - "profile_requirements has tenant_id column with RLS policy"
  - "All 5 tables have RLS enabled with tenant_isolation policy"
  - "Migration grants SELECT/INSERT/UPDATE/DELETE to bryton_app on all new tables"
  - "models/__init__.py imports all 5 new models without overwriting existing imports"
---

<objective>
Create the database foundation for Phase 3: ORM models for contracts, rate cards, profile catalogue, profile requirements, and a demand stub table. Write the Alembic migration with RLS policies, and define all Pydantic v2 request/response schemas.

Purpose: Establish the data layer that Plans 02 and 03 will build API endpoints on top of. Every downstream plan in this phase depends on these models and schemas existing.

Output: 5 ORM models, 2 schema modules, 1 migration, updated models/__init__.py
</objective>

<execution_context>
@/home/przem/.claude/forge-cli/workflows/execute-plan.md
@/home/przem/.claude/forge-cli/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-contracts-rate-cards-profile-catalogue/03-RESEARCH.md
@backend/app/models/__init__.py
@backend/app/models/tenant.py
@backend/app/models/approval.py
@backend/app/models/sfia_level.py
@backend/app/database.py
@backend/app/schemas/approval.py
@backend/alembic/versions/004_approvals_and_audit.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ORM models and Alembic migration</name>
  <files>
    backend/app/models/contract.py
    backend/app/models/rate_card.py
    backend/app/models/profile_catalogue.py
    backend/app/models/profile_requirement.py
    backend/app/models/demand.py
    backend/app/models/__init__.py
    backend/alembic/versions/005_contracts_profiles.py
  </files>
  <action>
    Create 5 new ORM models following the exact pattern established in `models/approval.py` and `models/tenant.py` (import Base from app.database, use Mapped[] with mapped_column, UUID PK with default=uuid.uuid4, DateTime with server_default=func.now()).

    **1. `backend/app/models/contract.py` — Contract model:**
    Table name: `contracts`. Columns from ARCHITECTURE.md Section 4.3:
    - `id` UUID PK (default uuid4)
    - `tenant_id` UUID NOT NULL FK to tenants(id) ondelete RESTRICT
    - `reference` String(100) NOT NULL
    - `title` String(255) NOT NULL
    - `lot_number` String(50) nullable
    - `start_date` Date NOT NULL
    - `end_date` Date NOT NULL
    - `max_value` Numeric(15,2) nullable
    - `currency` String(3) default 'EUR'
    - `status` String(20) default 'active' — values: active, expired, suspended
    - `created_at` DateTime(timezone=True) server_default=func.now()
    - `updated_at` DateTime(timezone=True) server_default=func.now() onupdate=func.now()
    Note: OMIT sla_config_id — SLA tables don't exist yet. Will be added in Phase 9.

    **2. `backend/app/models/rate_card.py` — RateCardEntry model:**
    Table name: `rate_cards`. Columns:
    - `id` UUID PK
    - `contract_id` UUID NOT NULL FK to contracts(id) ondelete CASCADE
    - `profile_id` UUID NOT NULL FK to profile_catalogue(id) ondelete RESTRICT
    - `sfia_level` Integer NOT NULL — add CheckConstraint("sfia_level BETWEEN 1 AND 7"). Do NOT use a UUID FK to sfia_levels.
    - `max_daily_rate` Numeric(10,2) NOT NULL — use `from sqlalchemy import Numeric`
    - `currency` String(3) default 'EUR'
    - `effective_from` Date NOT NULL
    - `effective_to` Date NOT NULL
    - `created_at` DateTime(timezone=True) server_default=func.now()
    Add __table_args__ with UniqueConstraint("contract_id", "profile_id", "sfia_level", "effective_from", name="uq_rate_card_entry")

    **3. `backend/app/models/profile_catalogue.py` — ProfileCatalogue model:**
    Table name: `profile_catalogue`. Columns:
    - `id` UUID PK
    - `tenant_id` UUID nullable FK to tenants(id) — nullable allows global profiles
    - `contract_id` UUID nullable FK to contracts(id) — nullable allows tenant-level profiles
    - `code` String(20) NOT NULL
    - `title` String(255) NOT NULL
    - `description` Text nullable
    - `sfia_level_min` Integer NOT NULL — CheckConstraint("sfia_level_min BETWEEN 1 AND 7")
    - `sfia_level_max` Integer NOT NULL — CheckConstraint("sfia_level_max BETWEEN 1 AND 7")
    - `min_years_exp` Integer NOT NULL default 0
    - `min_education` String(50) nullable
    - `required_clearance` String(20) nullable
    - `is_active` Boolean default True
    - `created_by` UUID nullable FK to users(id)
    - `created_at`, `updated_at` (standard timestamps)
    Add __table_args__ with UniqueConstraint("contract_id", "code", name="uq_profile_code_contract")

    **4. `backend/app/models/profile_requirement.py` — ProfileRequirement model:**
    Table name: `profile_requirements`. Columns:
    - `id` UUID PK
    - `profile_id` UUID NOT NULL FK to profile_catalogue(id) ondelete CASCADE
    - `tenant_id` UUID NOT NULL FK to tenants(id) — denormalised for RLS (see RESEARCH.md Pitfall 2)
    - `req_type` String(20) NOT NULL — values: skill, certification, language, clearance, education
    - `description` String(255) NOT NULL
    - `is_mandatory` Boolean default True
    - `min_cefr_level` String(2) nullable — only used when req_type='language'
    - `created_at` DateTime(timezone=True) server_default=func.now()

    **5. `backend/app/models/demand.py` — Demand STUB model:**
    This is a MINIMAL stub. Phase 5 will add remaining columns via ALTER TABLE.
    Table name: `demands`. Columns:
    - `id` UUID PK
    - `tenant_id` UUID NOT NULL FK to tenants(id) ondelete RESTRICT
    - `profile_id` UUID nullable FK to profile_catalogue(id) — the profile this demand was created from
    - `profile_snapshot` JSONB nullable — stores serialised profile at demand creation time for diff
    - `created_at`, `updated_at` (standard timestamps)

    **6. Update `backend/app/models/__init__.py`:**
    READ the current file first, then APPEND — do not overwrite existing content.
    Add imports for all 5 new models and update __all__ list:
    ```python
    from app.models.contract import Contract  # noqa: F401
    from app.models.rate_card import RateCardEntry  # noqa: F401
    from app.models.profile_catalogue import ProfileCatalogue  # noqa: F401
    from app.models.profile_requirement import ProfileRequirement  # noqa: F401
    from app.models.demand import Demand  # noqa: F401
    ```
    Extend __all__ with: "Contract", "RateCardEntry", "ProfileCatalogue", "ProfileRequirement", "Demand"

    **7. Create `backend/alembic/versions/005_contracts_profiles.py`:**
    Follow the exact pattern from `004_approvals_and_audit.py`. Revision: "005", down_revision: "004".
    
    Create tables in this exact dependency order (FK-safe):
    1. contracts (FK to tenants only)
    2. profile_catalogue (FK to tenants, contracts)
    3. demands (FK to tenants, profile_catalogue)
    4. profile_requirements (FK to profile_catalogue, tenants)
    5. rate_cards (FK to contracts, profile_catalogue)

    For EACH table:
    - Create indexes on tenant_id and any FK columns
    - Enable RLS: `ALTER TABLE {name} ENABLE ROW LEVEL SECURITY` + `FORCE ROW LEVEL SECURITY`
    - Create tenant_isolation policy matching exact pattern from 004 migration
    - Grant SELECT, INSERT, UPDATE, DELETE to bryton_app

    Additional indexes:
    - `rate_cards`: composite index on (contract_id, profile_id, sfia_level, effective_from) for ceiling lookup performance
    - `profile_catalogue`: partial unique index for tenant-level profiles: `CREATE UNIQUE INDEX uq_profile_code_tenant ON profile_catalogue(tenant_id, code) WHERE contract_id IS NULL`

    Write a full downgrade() that drops tables in reverse order, drops policies and indexes first.
  </action>
  <verify>
    Run BOTH of these checks:
    1. `cd /home/przem/bryton-ai-cv-app/backend && python -c "from app.models import Contract, RateCardEntry, ProfileCatalogue, ProfileRequirement, Demand; print('All models imported successfully')"` — must print success message without import errors.
    2. `cd /home/przem/bryton-ai-cv-app/backend && python -c "
import ast, sys
with open('alembic/versions/005_contracts_profiles.py') as f:
    tree = ast.parse(f.read())
funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]
assert 'upgrade' in funcs, 'Missing upgrade()'
assert 'downgrade' in funcs, 'Missing downgrade()'
print('Migration 005 parsed OK: upgrade + downgrade present')
"` — must confirm migration has both functions and is valid Python.
  </verify>
  <done>All 5 ORM models exist with correct column types, FKs, and constraints. Migration 005 creates tables in FK-safe dependency order (contracts -> profile_catalogue -> demands -> profile_requirements -> rate_cards) with RLS policies and indexes. models/__init__.py exports all new models alongside existing ones.</done>
</task>

<task type="auto">
  <name>Task 2: Create Pydantic v2 schemas for contracts, rate cards, and profiles</name>
  <files>
    backend/app/schemas/contract.py
    backend/app/schemas/profile.py
  </files>
  <action>
    Create Pydantic v2 schemas following the exact pattern from `schemas/approval.py` (ConfigDict(from_attributes=True), field_validator for enums, Optional fields).

    **1. `backend/app/schemas/contract.py`:**
    
    `ContractCreate(BaseModel)`:
    - reference: str (max_length=100)
    - title: str (max_length=255)
    - lot_number: str | None = None
    - start_date: date
    - end_date: date
    - max_value: Decimal | None = None
    - currency: str = "EUR" (max_length=3)
    - Add field_validator on status to validate values: active, expired, suspended (default 'active')
    - Add model_validator to check end_date >= start_date

    `ContractUpdate(BaseModel)`:
    - All fields optional. Same validators where applicable.

    `ContractResponse(BaseModel)`:
    - model_config = ConfigDict(from_attributes=True)
    - All Contract columns as typed fields. id: uuid.UUID, tenant_id: uuid.UUID, created_at: datetime, updated_at: datetime, etc.

    `RateCardEntryCreate(BaseModel)`:
    - profile_id: uuid.UUID
    - sfia_level: int (ge=1, le=7 via Field())
    - max_daily_rate: Decimal (gt=0)
    - currency: str = "EUR"
    - effective_from: date
    - effective_to: date
    - Add model_validator: effective_to >= effective_from

    `RateCardEntryUpdate(BaseModel)`:
    - max_daily_rate: Decimal | None = None (gt=0 if provided)
    - currency: str | None = None
    - effective_from: date | None = None
    - effective_to: date | None = None

    `RateCardEntryResponse(BaseModel)`:
    - model_config = ConfigDict(from_attributes=True)
    - All RateCardEntry columns as typed fields
    - margin: Decimal | None = None — this is the computed field (ceiling - cost_rate). Set to None for Customer role. This field is NOT in the ORM model — it's populated by the API layer.

    **2. `backend/app/schemas/profile.py`:**

    `ProfileRequirementCreate(BaseModel)`:
    - req_type: str — validate against: skill, certification, language, clearance, education
    - description: str (max_length=255)
    - is_mandatory: bool = True
    - min_cefr_level: str | None = None (max_length=2) — only when req_type='language'

    `ProfileRequirementResponse(BaseModel)`:
    - model_config = ConfigDict(from_attributes=True)
    - id: uuid.UUID, profile_id: uuid.UUID, tenant_id: uuid.UUID, req_type: str, description: str, is_mandatory: bool, min_cefr_level: str | None, created_at: datetime

    `ProfileCreate(BaseModel)`:
    - contract_id: uuid.UUID | None = None
    - code: str (max_length=20)
    - title: str (max_length=255)
    - description: str | None = None
    - sfia_level_min: int (ge=1, le=7)
    - sfia_level_max: int (ge=1, le=7)
    - min_years_exp: int = 0 (ge=0)
    - min_education: str | None = None
    - required_clearance: str | None = None
    - requirements: list[ProfileRequirementCreate] = [] — nested creation of requirements
    - Add model_validator: sfia_level_max >= sfia_level_min

    `ProfileUpdate(BaseModel)`:
    - All fields optional except code (immutable). Same validators.

    `ProfileResponse(BaseModel)`:
    - model_config = ConfigDict(from_attributes=True)
    - All ProfileCatalogue columns + requirements: list[ProfileRequirementResponse] = []

    `DemandDefaultsResponse(BaseModel)`:
    - Pre-fill dict for demand creation. Fields matching demand form:
    - sfia_level_min: int, sfia_level_max: int, min_years_exp: int
    - required_skills: str | None (from baseline skills in profile requirements)
    - min_education: str | None
    - required_clearance: str | None
    - languages: list[dict] (from language requirements)
    - certifications: list[str] (from certification requirements)
    - profile_snapshot: dict — the full serialised profile for storing in demand.profile_snapshot

    `ProfileDiffEntry(BaseModel)`:
    - field: str
    - profile_value: Any
    - demand_value: Any

    `ProfileDiffResponse(BaseModel)`:
    - deviations: list[ProfileDiffEntry]
    - has_deviations: bool

    `ComplianceItem(BaseModel)`:
    - req_id: uuid.UUID
    - req_type: str
    - description: str
    - is_mandatory: bool
    - status: str — MET, PARTIALLY_MET, NOT_MET

    `ComplianceCheckResponse(BaseModel)`:
    - overall: str — MET, PARTIALLY_MET, NOT_MET
    - items: list[ComplianceItem]

    Use `from decimal import Decimal` for all monetary fields. Use `from datetime import date, datetime` for date fields.
  </action>
  <verify>
    Run `cd /home/przem/bryton-ai-cv-app/backend && python -c "from app.schemas.contract import ContractCreate, ContractResponse, RateCardEntryCreate, RateCardEntryResponse; from app.schemas.profile import ProfileCreate, ProfileResponse, DemandDefaultsResponse, ComplianceCheckResponse; print('All schemas imported')"` — must succeed.
  </verify>
  <done>All Pydantic v2 schemas exist for contracts, rate cards, profiles, requirements, demand defaults, profile diff, and compliance check. Validation rules enforce date ordering, SFIA range, positive rates, and valid enum values.</done>
</task>

</tasks>

<verification>
- `python -c "from app.models import Contract, RateCardEntry, ProfileCatalogue, ProfileRequirement, Demand"` succeeds
- `python -c "from app.schemas.contract import ContractCreate, RateCardEntryCreate; from app.schemas.profile import ProfileCreate, ComplianceCheckResponse"` succeeds
- Migration file 005 exists with upgrade() and downgrade() functions and parses as valid Python
- models/__init__.py contains all original + 5 new model imports
</verification>

<success_criteria>
- All 5 ORM models correctly define their columns, FKs, and constraints matching ARCHITECTURE.md
- Migration 005 creates tables in FK-safe dependency order with RLS policies, indexes, and grants
- Pydantic schemas enforce validation (date ordering, SFIA range 1-7, positive rates, valid enums)
- RateCardEntryResponse includes optional margin field for role-based visibility
- No existing model imports in __init__.py are lost or overwritten
</success_criteria>

<output>
After completion, create `.planning/phases/03-contracts-rate-cards-profile-catalogue/03-01-SUMMARY.md`
</output>
